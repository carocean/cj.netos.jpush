group 'cj.netos.jpush'
version engineer_version
apply plugin:'application'
mainClassName = "cj.netos.jpush.terminal.start.Entrypoint"
sourceSets {
    main {
        java{
            srcDir "$projectDir/src"
        }
        resources{
            srcDir "$projectDir/src"
        }
    }
}
sourceCompatibility = 1.8
targetCompatibility = 1.8
tasks.withType(JavaCompile) {
    options.encoding = "UTF-8"
}
repositories {
    mavenCentral();
}
jar {
    manifest {
        attributes 'Manifest-Version': version,
                'Class-Path': '.',
                'Main-Class':'cj.netos.jpush.terminal.start.Entrypoint',
                'DynamicImport-Package':'*'
    }
}

dependencies {
    compile group: 'commons-cli', name: 'commons-cli', version: '1.4'
    compile group: 'log4j', name: 'log4j', version: '1.2.17'
    compile project (':cj.netos.jpush.corelib')

}
task runnableJar(dependsOn:':cj.netos.jpush.terminal.node:release',type: Jar) {
    manifest {
        attributes 'Implementation-Title': 'Gradle Jar File Example',
                'Implementation-Version': version,
                'Main-Class': 'cj.netos.jpush.terminal.start.Entrypoint'
    }
    baseName = 'nodestart'
    from { configurations.compile.collect { it.isDirectory() ? it : zipTree(it) } }
    with jar
}

task releaseTerminalStart(dependsOn: runnableJar,type: Copy) {
    dependsOn "build"
    from 'build/libs/'
    into '../cmdtools/terminal'
    include '**/nodestart-*.jar'
    exclude '**/*-javadoc.jar','**/*-sources.jar'
}

task release(type: Copy) {
    dependsOn "releaseTerminalStart",":cj.netos.jpush.corelib:release",":cj.netos.jpush.terminal.node:release"
    doLast {
        println 'gateway 环境发布完毕!'
    }
}
tasks.getByPath(':cj.netos.jpush.terminal.node:release').mustRunAfter tasks.getByPath(':cj.netos.jpush.corelib:release')